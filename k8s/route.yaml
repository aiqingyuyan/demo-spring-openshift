apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: pipelines-demo-app
  name: pipelines-demo-app
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: pipelines-demo-app
    weight: 100
