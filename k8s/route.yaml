apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: demo-spring-openshift
  name: demo-spring-openshift
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: demo-spring-openshift
    weight: 100
